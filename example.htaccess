RewriteEngine On

# Rediriger les utilisateurs non authentifiés vers WordPress pour l'authentification
RewriteCond %{HTTP_COOKIE} !wordpress_logged_in_
RewriteRule ^(.*)$ https://wrap.rocks/wp-login.php?redirect_to=%{REQUEST_URI} [L,R=302]
